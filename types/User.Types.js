/**
 * Types are a way for Javascript to implement type checking without typescript. It can be a little confusing at first, but it prevents you from misspelling
 * actions and sending data to the wrong reducer. Just copy this pattern for each action file
 */
export const AUTH_USER = 'AUTH_USER';
export const CLEAR_USER = 'CLEAR_USER';
